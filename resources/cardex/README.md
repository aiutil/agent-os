Place the 7x6 Cardex sprite sheet here as `cards.png`.

Expected source:
- 42 cards
- 7 columns x 6 rows
- every cell has the same width and height
- no gaps or outer margin

The app reads `/cardex/cards.png` automatically. If the file is missing, it falls back to generated icon cards.
